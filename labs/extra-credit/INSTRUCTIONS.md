# Extra credit lab instructions

Same core logic as the polling enhancer and fixer.
The trigger may be GitHub Actions, ngrok, or a DigitalOcean Droplet.

One FastAPI `/github-webhook`. Verify the signature. Route events. Stop at max attempts.
Do not skip Module 2 to work on this.

Stop at max attempts. Log every decision. Do not skip Module 2 to work on this.
