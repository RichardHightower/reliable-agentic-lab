# Extra credit stub

Not on the Saturday clock.

Fill `scripts/groom_ticket.py` and `scripts/fix_pr.py`.
Copy `workflows/` onto your fork as `.github/workflows/`.
Working example: `solutions/extra_credit/`.

Claude Code is not required. See [../HOW-TO-RUN.md](../HOW-TO-RUN.md).
