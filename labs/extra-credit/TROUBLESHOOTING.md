# Extra credit lab troubleshooting

If Actions comments in a loop, add `agent-in-progress` handling first.
If ngrok deliveries are HTML, the free interstitial is in the way.
If you are behind on Saturday, abandon extra credit and copy `solutions/`.
