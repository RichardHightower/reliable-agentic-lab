# Troubleshooting

- Hidden grader import errors: export `PYTHONPATH` to the CRM you are grading.
- Stub still raises `NotImplementedError`: paste a prompt from `prompts/` and let the tool fill only this folder.
- Do not edit `solutions/m2-harness/graders/`. Those tests are the contract.
