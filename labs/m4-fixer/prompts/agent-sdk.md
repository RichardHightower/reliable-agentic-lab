# Prompt for the Claude Agent Software Development Kit (SDK)

Agent watches failing checks. Tools: read failure log, edit the PR branch files, re-run pytest.
Max iterations: 3. After that, comment and stop. Do not loop forever.

Unattended mode: no human approval for the fix attempt. Human still merges.
