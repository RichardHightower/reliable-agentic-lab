# Prompt for OpenCode

Claude Code is not required. Same task as `claude-code.md`.

From the repo root, headless:

```bash
opencode run --dir . "$(cat labs/m3-enhancer/prompts/claude-code.md)"
```

Interactive: start OpenCode in this clone and paste `claude-code.md`.

Full tool notes: [labs/HOW-TO-RUN.md](../../HOW-TO-RUN.md).
