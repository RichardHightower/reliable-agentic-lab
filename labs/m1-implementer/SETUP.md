# Setup

Use the repo virtual environment from the root `SETUP.md`.

No extra install. Hidden grader is `solutions/m2-harness/graders/test_due_date_contract.py`. Starter app is `solutions/m1-implementer/starter_crm`.
