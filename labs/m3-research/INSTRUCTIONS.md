# Instructions

From the repo root:

```bash
python labs/m1-implementer/loop.py
```

Fill `apply_change()` so the hidden grader goes green.
Write a PR body that links back to T001.


Pass means: JSON with `passed: true` and a `PR.md` that links to T001

If you stall, open `FALL-BEHIND.md`.
