# Module 1 setup

Repo-root venv. Same CRM requirements.

```bash
pip install -r solutions/crm/requirements.txt
```

No extra packages. No API key required for the reference loop.

`starter_crm/` is the CRM *without* due dates.
`solutions/m2-harness/fixtures/t001-pass/` is the patch the loop applies.
