# Optional. The Droplet's /opt/agents/.env is the normal place.
# Copy this only if you run sol1_enhancer by hand on the box.
#
#     cp .env.example .env
#
# Never commit .env. GITHUB_TOKEN and ANTHROPIC_API_KEY live in the
# Droplet env, not here.

ANTHROPIC_API_KEY=
