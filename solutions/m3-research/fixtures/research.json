{
  "summary": "Optional UTC ISO due dates on CRM sales tasks are a testable contract. Null must stay valid. Overdue is open plus date before today UTC.",
  "must_include": ["optional", "UTC", "ISO 8601", "overdue"],
  "required_tokens": ["optional", "utc", "iso", "overdue", "null", "mcp", "model context protocol"],
  "forbidden": ["required due date", "local time", "must be required"],
  "claims": [
    {"text": "due_date is optional", "aliases": ["optional due date", "null due date", "optional utc"]},
    {"text": "store UTC ISO 8601", "aliases": ["iso 8601", "stored utc", "utc date"]},
    {"text": "overdue means open and before today UTC", "aliases": ["overdue", "due_before"]}
  ],
  "sources": [
    {
      "title": "Ready ticket T001",
      "quote": "sales_tasks.due_date is a date field, optional, ISO 8601, stored UTC."
    },
    {
      "title": "Hidden grader contract",
      "quote": "Overdue means status is open and due_date is before today's UTC date."
    }
  ]
}
