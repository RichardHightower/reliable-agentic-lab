# The optional take-home labs only. Nothing in the five hours needs these,
# and `task test` passes without them.
claude-agent-sdk>=0.1.0
deepagents>=0.7.0
langchain>=1.0.0
# Module 3 only. Loads the servers in .mcp.json for the research loop.
# Without it the loop falls back to the Perplexity REST API, then the fixture.
langchain-mcp-adapters>=0.1.0
langfuse>=3.0.0,<4.0.0
