# Branches

Target shape by Friday:

| Branch | Meaning |
| --- | --- |
| `main` | Docs and the current starter proof |
| `known-good` | Instructor due-date implementation. Hidden tests pass |
| `start-m1` | CRM plus one ready ticket plus a stub loop |
| `done-m1` | Working implementer once |
| `start-m2` | done-m1 plus harness stubs |
| `done-m2` | Harness that fails then passes |
| `start-m3` | done-m2 plus MCP stubs |
| `done-m3` | Enhancer marks a vague ticket ready |
| `start-m4` | done-m3 plus Actions stub |
| `done-m4` | Actions run and a mergeable pull request |

Sunday 23 August only creates `main` and `known-good`.
The module branches are cut when that lab exists. Do not invent empty module branches.
