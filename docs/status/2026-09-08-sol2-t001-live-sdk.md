---
date: 2026-09-08
doer: sdk
ticket: T001
epic: 414
story: 425
ticket_ref: 444
fix_ref: 543
---

# Sol 2 live T001 trace: sdk

Doer: sdk
Gate: escalate
Spend: $1.3657 (cap $3.00, applied via `SOL2_E2E_MAX_USD=3`)
Unknown-spend turns: 0
Tests written in code phase: no
Main sha: 2a3e1f66988334148bf091c5eeeea3d35b2023b0

Two test-implementer queries ran and both spent real money: $0.6631 and
$0.7026. Each hit the per-query SDK budget ceiling mid-turn (`terminal_reason:
budget_exhausted`) before it produced a final answer, so neither wrote a
test and the loop escalated on "the test implementer backend did not
answer" rather than the generic red-gate wording. The code phase never
started, so it never had a chance to write under `tests/**`. This run
used the worktree-cwd fix from #543: `ClaudeAgentOptions.cwd` and the
scope hook were rooted at the run's own worktree, not the fixture clone.
Both queries' raw event logs are checked in beside this file
(`last-sdk-e2e-raw-0-test.txt`, `last-sdk-e2e-raw-1-test.txt`), copied via
`SOL2_E2E_RAW_LOG_DIR` with the operator's home directory and any
key-shaped text stripped.

The round-4 confirming run for this doer is
`2026-09-08-sol2-t001-live-sdk-round4.md`: both queries ended on the
per-query dollar ceiling in about a minute, and the fixture's own
`.claude/settings.json` denied every test write either query
attempted, before the red gate could see one.
